{
  "name": "tanvo-agency",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "generate": "node scripts/build-pages.mjs",
    "predev": "npm run generate",
    "dev": "vite",
    "prebuild": "npm run generate",
    "build": "vite build",
    "preview": "vite preview",
    "seo:check": "node scripts/seo-check.mjs"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^6.0.7"
  }
}
